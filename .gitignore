node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json
coverage/
.worktrees/
.idea

# clerk configuration (can include secrets)
.clerk

# Handoff docs — keep the folders, ignore their (potentially sensitive) contents
.claude/handoffs/*
!.claude/handoffs/.gitkeep
.cursor/handoffs/*
!.cursor/handoffs/.gitkeep

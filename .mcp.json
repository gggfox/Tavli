{
	"mcpServers": {
		"convex": {
			"command": "npx",
			"args": ["-y", "convex@latest", "mcp", "start"]
		},
		"stripe": {
			"command": "npx",
			"args": ["-y", "@stripe/mcp", "--tools=all"],
			"env": {
				"STRIPE_SECRET_KEY": "${STRIPE_SECRET_KEY}"
			}
		}
	}
}
